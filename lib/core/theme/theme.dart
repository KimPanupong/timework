import 'package:flutter/material.dart';

const primaryColor = Color(0xFF0084CE);
const black = Colors.black;
const danger = Colors.red;
const background = Color(0XFFF5F5F5);
const white = Colors.white;
const disable = Color(0xFFDFE3E8);
const textDisable = Color(0xFFA5AFBA);
const text800 = Color(0xFF02598A);

const bgLinerGradient = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    primaryColor,
    black,
    danger,
    background,
    white,
    textDisable,
    text800
  ],
);
